{
  "name": "pixitest",
  "version": "1.0.0",
  "description": "* `npm run start` - runs the compiler and a server at the same time in dev mode with HMR (Hot Module Replacement) 🔥. * `npm run build` - runs the compiler once and generates a production build. * `npm run build_serve` - it makes a build and serves it to port 8080.",
  "scripts": {
    "test": "jest",
    "start": "npm run clean && parcel src/index.html",
    "build": "npm run clean && parcel build src/index.html --public-url ./",
    "build_serve": "npm run build && http-server ./dist",
    "clean": "rimraf ./dist ./.cache"
  },
  "author": "Llorenç Pujol Ferriol",
  "license": "MIT",
  "dependencies": {
    "pixi.js": "~5.2.0",
    "prettier": "^2.0.4"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "babel-preset-es2015": "^6.24.1",
    "http-server": "0.11.1",
    "jest": "~24.9.0",
    "parcel-bundler": "1.11.0",
    "rimraf": "^2.6.2",
    "ts-jest": "^24.2.0",
    "typescript": "^3.9.10"
  },
  "main": "jest.config.js",
  "bin": {
    "pixitest": "bin/main.10bdb33d.js"
  },
  "keywords": []
}
